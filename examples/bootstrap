#!/bin/sh
rm -f config.cache
echo autoheader...
autoheader
echo libtoolize...
libtoolize --force --copy
echo aclocal...
aclocal
echo automake...
automake --force --foreign --add-missing --copy
echo autoconf...
autoconf
echo "Now run configure with any arguments necessary"
